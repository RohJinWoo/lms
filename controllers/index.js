const notice = require('./notice');
const student = require('./student');
const professor = require('./professor');
const user = require('./user');
const email = require('./email');

module.exports = {
    notice, student, professor, user, email
};