<!DOCTYPE html>
<html>
  <head>
    <title><%= obj.title !== undefined ? obj.title : '' %></title>
  </head>
  <body>
    <form id="form">
      <h1><%= obj.title %></h1>
      <div>
        <input type="text" id="u_id" name="u_id" placeholder="ID" value=<%= obj.u_id !== undefined ? obj.u_id : '' %>>
      </div>
      <div>
        <input type="password" id="password" name="password" placeholder="비밀번호">
      </div>
      <div>
        <button type="button" onclick="login('std')">학습자 로그인</button>
        <button type="button" onclick="login('prof')">교육자 로그인</button>
      </div>
      
      <div>
        <button type="button" onclick="sign_up()">회원가입</button>
        <div role="group">
          <button type="button" onclick="find_id()">ID 찾기</button>
          <button type="button" onclick="find_pw()">패스워드 찾기</button>
        </div>
      </div>
    </form>
  </body>
  <script src="javascripts/util.js"></script>
  <script src="javascripts/user_req.js"></script>
</html>
