<!DOCTYPE html>
<html>
  <head>
    <title><%= obj.title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <style type="text/css">
        body > nav, body > section {
            float: left;
            height : 200px;
        }
        .users{
            float: left;
        }
        nav{
            background: lightgreen;
        }
        header{
            background: lightblue;
        }
        section{
            background: lightpink;
        }
        section > article:nth-child(1){
            background: lightsalmon;
        }
        section > article:nth-child(2){
            background: lightcoral;
        }
        section > article:nth-child(3){
            background: lightcyan;
        }
    </style>
  </head>
  <body>
        <nav include-ejs="nav.ejs">
            <% include ./nav %>
        </nav>
        <section>
            <header>
                공백 및 타이틀
            </header>
            <section>
                <article>
                    <b>공지사항</b>
                    <div><%= obj.notice[0].title %> | <%= obj.notice[0].date %></div>
                    <div><%= obj.notice[1].title %> | <%= obj.notice[1].date %></div>
                </article>
                <article class="users" id="std">강의진행도
                    <div><%= obj.sub_prog[0].cource_name %> | <%= obj.sub_prog[0].progress %></div>
                    <div><%= obj.sub_prog[1].cource_name %> | <%= obj.sub_prog[1].progress %></div>
                </article>
                <article class="users">내용들어가</article>
            </section>
        </section>
  </body>
</html>
