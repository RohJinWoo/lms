<!DOCTYPE html>
<html>
    <head>
        <title>xxx</title>
    </head>
    <body>
        get 방식
        <div>
            <% for(let i = 0; i < obj.list.length; i++){ %>
            <p><a href='./list?dir=<%= obj.dir + "/" + obj.list[i] %>'><%= obj.list[i] %></a></p>
            <% } 
            if(obj.dir !== ""){
            %>
            <a href='#' onclick="back('back_g')">뒤로가기(미완)</a>
            <% } %>
        </div><br/>
        ajax post방식
        <div class="list" id="list"></div>
    </body>
    <script src="/javascripts/util.js"></script>
    <script src="/js/axios.min.js"></script>
    <script src="/javascripts/file/search_dir.js"></script>
</html>